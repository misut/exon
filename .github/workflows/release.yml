name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-15
            platform: aarch64-apple-darwin
          - os: ubuntu-24.04
            platform: x86_64-linux-gnu
          - os: ubuntu-24.04-arm
            platform: aarch64-linux-gnu
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6

      - name: Install intron
        run: |
          gh release download v0.17.0 -R misut/intron -p "intron-*-${{ matrix.platform }}.tar.gz"
          tar xzf intron-*.tar.gz && rm intron-*.tar.gz
          mkdir -p "$HOME/.intron/bin" && mv intron "$HOME/.intron/bin/"
          echo "$HOME/.intron/bin" >> $GITHUB_PATH
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Install toolchains
        run: |
          intron install llvm 22.1.2
          intron install cmake 4.3.1
          if [ "$(uname -m)" = "aarch64" ] && [ "$RUNNER_OS" = "Linux" ]; then
            sudo apt-get update && sudo apt-get install -y ninja-build
          else
            intron install ninja 1.13.2
          fi

      - name: Bootstrap and release build
        run: |
          eval "$(intron env)"
          LLVM_ROOT="$HOME/.intron/toolchains/llvm/22.1.2"
          MODULES_JSON=$(find "$LLVM_ROOT" -name "libc++.modules.json" -print -quit)
          CMAKE_ARGS="-Wno-dev -B build -G Ninja"
          CMAKE_ARGS="$CMAKE_ARGS -DCMAKE_CXX_COMPILER=$LLVM_ROOT/bin/clang++"
          if [ -n "$MODULES_JSON" ]; then
            CMAKE_ARGS="$CMAKE_ARGS -DCMAKE_CXX_STDLIB_MODULES_JSON=$MODULES_JSON"
          fi
          if [ "$RUNNER_OS" = "macOS" ]; then
            CMAKE_ARGS="$CMAKE_ARGS -DCMAKE_OSX_SYSROOT=$(xcrun --show-sdk-path)"
          fi
          if [ "$RUNNER_OS" = "Linux" ]; then
            LIBCXX_DIR=$(dirname "$(find "$LLVM_ROOT" -name "libc++.so" -print -quit)")
            CMAKE_ARGS="$CMAKE_ARGS -DCMAKE_CXX_FLAGS=-stdlib=libc++"
            CMAKE_ARGS="$CMAKE_ARGS \"-DCMAKE_EXE_LINKER_FLAGS=-stdlib=libc++ -lc++abi -Wl,-rpath,$LIBCXX_DIR\""
            export LD_LIBRARY_PATH="$LIBCXX_DIR:${LD_LIBRARY_PATH:-}"
          fi
          eval cmake $CMAKE_ARGS
          cmake --build build --target exon
          ./build/exon build --release

      - name: Package
        run: |
          tar -czf exon-${{ github.ref_name }}-${{ matrix.platform }}.tar.gz \
            -C .exon/release exon

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: exon-${{ matrix.platform }}
          path: exon-*.tar.gz

  build-windows:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v6

      - name: Setup MSVC
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install intron
        shell: pwsh
        run: |
          gh release download v0.17.0 -R misut/intron -p "intron-*-x86_64-pc-windows-msvc.zip"
          Expand-Archive -Path intron-*.zip -DestinationPath .
          Remove-Item intron-*.zip
          New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\.intron\bin" | Out-Null
          Move-Item -Path intron.exe -Destination "$env:USERPROFILE\.intron\bin\intron.exe"
          echo "$env:USERPROFILE\.intron\bin" >> $env:GITHUB_PATH
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Install toolchains
        shell: pwsh
        run: |
          intron install cmake 4.3.1
          intron install ninja 1.13.2
          echo "$env:USERPROFILE\.intron\toolchains\cmake\4.3.1\bin" >> $env:GITHUB_PATH
          echo "$env:USERPROFILE\.intron\toolchains\ninja\1.13.2" >> $env:GITHUB_PATH

      - name: Bootstrap and release build
        shell: cmd
        run: |
          cmake -B build -G Ninja -DCMAKE_BUILD_TYPE=Release
          cmake --build build --target exon
          .\build\exon.exe build --release

      - name: Package
        shell: pwsh
        run: Compress-Archive -Path .exon/release/exon.exe -DestinationPath exon-${{ github.ref_name }}-x86_64-pc-windows-msvc.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: exon-x86_64-pc-windows-msvc
          path: exon-*.zip

  release:
    needs: [build, build-windows]
    runs-on: ubuntu-latest
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v8
        with:
          merge-multiple: true

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            exon-*.tar.gz
            exon-*.zip
          generate_release_notes: true
